# Search First (Thin)

Before implementing unfamiliar APIs or patterns, search first:

1. `Grep`/`Glob` codebase for existing implementations
2. `dev_memory(action="query", query="<keywords> pitfalls")` for known issues
3. If not found in codebase: `WebSearch` official docs
4. Present summary, proceed or wait for confirmation if conflicts found
